{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "type": "shell",
            "label": "clean",
            "command": "source",
            "args": [
                "../bin/clean"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": "build",
            "detail": "clean current project"
        },
        {
            "type": "shell",
            "label": "build",
            "command": "source",
            "args": [
                "../bin/build"
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "clear": true
            },
            "detail": "Build using cmake"
        },
    ]
}